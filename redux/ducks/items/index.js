import { combineReducers } from 'redux'
import sidebarReducer from '../ducks/sidebar/sidebarSlice'
import userReducer from '../ducks/user/userSlice'

const itemTypeReducer = combineReducers({
    //item types
})

export default combineReducers({
    //items
    // itemTypeReducer
})